GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GRAFANA_MCP_ENDPOINT=https://mcp.grafana.com/mcp
GRAFANA_URL=https://YOUR_STACK.grafana.net
# Optional only when using an MCP endpoint that accepts a pre-issued bearer token.
GRAFANA_MCP_BEARER_TOKEN=
